/*
What do you think the following code will output?

Bonus:
How can you reliably test if a value is NaN?
Use the isNaN() method to determine whether a value is NaN or not.
*/
let nanArray = [NaN];

console.log(nanArray[0] === NaN); // false
